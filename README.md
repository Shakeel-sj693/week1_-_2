# Build and Run Instructions

To build the project, use the following Maven command:

```bash
mvn clean install
```

To run any specific problem, navigate to the corresponding module directory and use:

```bash
mvn exec:java
```
